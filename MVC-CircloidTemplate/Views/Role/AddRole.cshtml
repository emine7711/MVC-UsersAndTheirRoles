
@{
    ViewBag.Title = "Rol Ekleme";
}
@section head{
    <style>
        #hata {
            color: red;
            font-size: 16px;
        }
    </style>
}
<div class="row">
    <div class="col-md-12 ">
        <div class="block">
            <div class="block-heading">
                <div class="main-text h2">
                    Rol Ekleme
                </div>
                <div class="block-controls">
                    <a href="/Role/AddRole"><span aria-hidden="true" class="icon icon-cross icon-size-medium block-control-remove"></span></a>
                    <span aria-hidden="true" class="icon icon-arrow-down icon-size-medium block-control-collapse"></span>
                </div>
            </div>
            <div class="block-content-outer">
                <div class="block-content-inner">
                    <form action="/Role/AddRole" method="post" role="form" enctype="multipart/form-data">
                        <div class="form-group">
                            @{
                                string message = ViewBag.Message;
                                if (message != null)
                                {
                                    if (message == "Başarılı")
                                    {
                                        <label class="text-success">@message</label>
                                    }
                                    else
                                    {
                                        <label class="text-danger">@message</label>
                                    }
                                }
                            }
                            </div>
                        <div>
                            <label for="exampleInputEmail1">Rol Adı</label>
                            <input type="text" class="form-control" id="exampleInputEmail1" name="role" placeholder="Rol Adı Giriniz">
                        </div>
                        <button type="submit" class="btn btn-primary roleAdd">Kaydet</button>
                        @*<span id="hata">@ViewBag.Message</span>*@
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
@*@section scripts{
        <script src="~/Content/assets/js/required/jquery-1.11.1.min.js"></script>
        <script>
            $(".roleAdd").click(function () {
                    var roleName = $(this).attr("data-roleName");
                    $.ajax({
                        type: "POST",
                        url: "/Role/AddRole/" + roleName,
                        alert(url);
                        success: function () {
                            alert(@ViewBag.Message);
                        },
                        error: function () {
                            alert("Ekleme Sırasında Hata Oluştu!");
                        },
                    })
            });
        </script>
    }*@
